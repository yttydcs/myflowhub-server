# 2026-03-15 - Server：对齐 Core 到 v0.4.5

## 变更背景 / 目标
- 背景：Core 发布 `v0.4.5`，包含 Windows RFCOMM 流式读写兼容修复。
- 目标：Server 依赖升级到 `myflowhub-core v0.4.5`，确保发行构建包含最新修复链路。

## 具体变更
- 修改 `go.mod`：
  - `github.com/yttydcs/myflowhub-core`：`v0.4.4` -> `v0.4.5`

## 任务映射
- `SERVER-1` 依赖升级
- `SERVER-2` 测试验证
- `SERVER-3` 文档归档
- `SERVER-4` 提交合并与 tag

## 验证
- `go test ./... -count=1`：通过

## 影响与回滚
- 影响：仅依赖版本升级，不改 Server 业务逻辑。
- 回滚：回退 `go.mod` 与本归档文档。
