# 2026-03-15 - Server：对齐 Core 到 v0.4.4

## 变更背景 / 目标
- 背景：Core 已发布 `v0.4.4`，修复 Windows RFCOMM 连接中止相关 transport 边界问题。
- 目标：将 Server 下游依赖统一到 `myflowhub-core v0.4.4`，确保发布构建默认包含该修复。

## 具体变更
- 修改 `go.mod`：
  - `github.com/yttydcs/myflowhub-core`：`v0.4.0` -> `v0.4.4`

## 任务映射
- `SERVER-1` 依赖升级
- `SERVER-2` 测试验证
- `SERVER-3` 文档归档
- `SERVER-4` 提交合并与 tag

## 验证
- `go test ./... -count=1`：通过

## 影响与回滚
- 影响：仅依赖版本升级，不改业务逻辑。
- 回滚：回退 `go.mod` 与本归档文档。
