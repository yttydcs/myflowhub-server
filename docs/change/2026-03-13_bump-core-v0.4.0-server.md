# 2026-03-13 - Server 对齐 Core v0.4.0

## 背景 / 目标
- `MyFlowHub-Core` 已发布 `v0.4.0`，包含 Link / Frame / HeaderRouter 内核收敛这一重大变更。
- 本次仅对 `MyFlowHub-Server` 做依赖版本收口，确保主分支使用可拉取的 `Core v0.4.0` 发布版本。

## 具体变更
- `go.mod`
  - `github.com/yttydcs/myflowhub-core: v0.3.1 -> v0.4.0`
- `go.sum`
  - 同步 `Core v0.4.0` 校验信息

## 设计决策与权衡
- 只做版本升级，不改 Server 运行时逻辑、RFCOMM 逻辑、子协议装配逻辑。
- 优先使用 `GOWORK=off` 验证，确保发布版本可被真实拉取，而不是依赖本地 workspace 偶然通过。
- 若升级暴露兼容问题，才进入最小必要修复；本次未触发。

## 验证
- 目录：`worktrees/chore-bump-core-v0.4.0-downstream/repo/MyFlowHub-Server`
- 命令：`$env:GOWORK='off'; go test ./... -count=1`
- 结果：通过

## 风险与回滚
- 风险较低：本次不改业务逻辑，仅做版本收口。
- 如需回滚，可回退本次依赖升级提交，并恢复 `myflowhub-core v0.3.1`。
