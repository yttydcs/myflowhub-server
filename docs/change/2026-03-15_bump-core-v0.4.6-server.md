# 变更背景 / 目标

对齐 Core 的 RFCOMM Windows 修复版本，确保 Server 侧依赖与 Win/SDK 保持一致，便于统一联调与发布。

# 具体变更内容

- `go.mod` / `go.sum`
  - `github.com/yttydcs/myflowhub-core` 从 `v0.4.5` 升级到 `v0.4.6`。

# 对应任务映射

- Task: 下游依赖对齐（Server）
  - 目标：统一到 Core `v0.4.6`。
  - 验收：`go test ./...` 通过。

# 关键设计决策与权衡

- 本次仅升级依赖，不改业务逻辑，降低回归风险。

# 测试与验证

- `go test ./...`

# 潜在影响与回滚

- 影响：依赖版本变化，行为以 Core `v0.4.6` 为准。
- 回滚：将 Core 依赖回退至 `v0.4.5` 并重新测试。
