# 2026-04-05 Server：发布 graph contract downstream v0.0.15

## 变更背景 / 目标
- 当前 `MyFlowHub-Server` 主线已经合入 flow graph contract closure 相关行为和文档：
  - 行为级 `TestIntegrationFlowRoundTrip`
  - flow spec 收口到 Proto canonical contract
  - 运行控制和 advanced graph/node round-trip 已在当前主线存在
- 但正式依赖仍停在 `myflowhub-proto v0.1.6` 与 `myflowhub-subproto/flow v0.1.4`，不能作为本轮 graph contract closure 的正式下游发布状态。
- 本次目标是把 Server 对齐到 `proto v0.1.7 + flow v0.1.5`，并正式发布 `v0.0.15`。

## 具体变更内容
- `go.mod`
  - `github.com/yttydcs/myflowhub-proto v0.1.6 -> v0.1.7`
  - `github.com/yttydcs/myflowhub-subproto/flow v0.1.4 -> v0.1.5`
- `go.sum`
  - 同步本轮 Proto / flow 版本的真实校验和
- 本次不改 Server 行为代码，只做 release-state 依赖收口
- 新发布 tag:
  - `v0.0.15`

## Requirements impact
`none`

## Specs impact
`none`

## Lessons impact
`none`

## Related requirements
- `D:\project\MyFlowHub3\worktrees\server-graph-contract-release-chain\docs\requirements\flow_data_dag.md`

## Related specs
- `D:\project\MyFlowHub3\worktrees\server-graph-contract-release-chain\docs\specs\flow.md`

## Related lessons
- `D:\project\MyFlowHub3\docs\lessons\cross-repo-semver-release.md`

## 对应 plan.md 任务映射
- `REL-3` - Server bump `proto/flow`, validate, tag `v0.0.15`

## 经验 / 教训摘要
- Server 作为下游消费方，正式发布前必须回到真实 semver 依赖，而不能继续停留在旧上游 patch。
- 对当前 flow 收口类变更，行为级 round-trip 测试比单纯 `go build` 更能证明 contract 已闭环。

## 可复用排查线索
- 症状
  - `go list -m github.com/yttydcs/myflowhub-proto` 仍是 `v0.1.6`
  - `go list -m github.com/yttydcs/myflowhub-subproto/flow` 仍是 `v0.1.4`
  - `GOWORK=off` 下 flow round-trip 测试与当前主线 contract 不一致
- 触发条件
  - 上游 graph contract 已发布新 patch，但 Server `go.mod` 没跟上
- 关键词
  - `GOWORK=off`
  - `TestIntegrationFlowRoundTrip`
  - `myflowhub-proto v0.1.7`
  - `myflowhub-subproto/flow v0.1.5`
- 快速检查
  - `go list -m github.com/yttydcs/myflowhub-proto`
  - `go list -m github.com/yttydcs/myflowhub-subproto/flow`
  - `GOWORK=off go test ./tests -run TestIntegrationFlowRoundTrip -count=1 -p 1`

## 关键设计决策与权衡
- 采用“依赖升级 + 行为级验证”的最小发布策略，不在 release workflow 里继续改 Server 运行时设计。
- 在外网不可用时，用临时本地 git 映射解析上游本地 tags，只作为验证手段，不写入仓库文件。

## 测试与验证方式 / 结果
- `go list -m github.com/yttydcs/myflowhub-proto`
  - 结果：`v0.1.7`
- `go list -m github.com/yttydcs/myflowhub-subproto/flow`
  - 结果：`v0.1.5`
- `GOWORK=off go test ./tests -run TestIntegrationFlowRoundTrip -count=1 -p 1`
  - 结果：通过

## 潜在影响
- 若外部下游仍锁在旧 Server tag，不会自动拿到本轮 graph contract 下游基线。

## 回滚方案
- 若 tag 尚未推送：
  - 删除本地 tag
  - 回退 `go.mod/go.sum` 与 archive 提交
- 若 tag 已推送：
  - 不重写已发布 tag，改发更高 patch 版本

## 子Agent执行轨迹
- 本轮未使用子Agent。
