# Server：升级 myflowhub-core 到 v0.3.1（RFCOMM 发布对齐）

## 背景 / 目标
- Core 已发布 `v0.3.1`，包含 RFCOMM transport。
- Server 主分支已接入 RFCOMM listener / parent dial，但 `go.mod` 仍指向 `myflowhub-core v0.3.0`。
- 目标：把 Server 依赖对齐到 `v0.3.1`，确保 `GOWORK=off` 可直接拉取并通过测试。

## 具体变更
- `go.mod`：`github.com/yttydcs/myflowhub-core v0.3.0 -> v0.3.1`
- `go.sum`：更新校验和

## 设计与权衡
- 只做依赖升级，不改 RFCOMM 逻辑与运行时行为。
- 发布 patch 版本 `v0.0.6`，延续当前 Server 仓的版本节奏。

## 测试与验证
- `cd repo/MyFlowHub-Server; $env:GOWORK='off'; go test ./... -count=1`

## 回滚
- revert 本次依赖升级提交；如已发布，使用更高 patch 版本修正。
